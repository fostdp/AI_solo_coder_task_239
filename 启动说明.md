# 校友通讯录更新系统 - 启动说明

## 项目结构

```
.
├── backend/                 # Node.js 后端
│   ├── config/
│   │   └── database.js      # 数据库配置
│   ├── middleware/
│   │   └── auth.js          # 认证中间件
│   ├── routes/
│   │   ├── auth.js          # 认证路由
│   │   ├── alumni.js        # 校友路由
│   │   └── admin.js         # 管理员路由
│   ├── scripts/
│   │   └── init-db.js       # 数据库初始化脚本
│   ├── data/                # 数据库文件目录
│   ├── .env                 # 环境变量
│   ├── package.json
│   └── server.js            # 服务器入口
│
└── frontend/                # React 前端
    ├── public/
    │   └── index.html
    ├── src/
    │   ├── components/
    │   │   └── Navbar.js    # 导航栏组件
    │   ├── contexts/
    │   │   └── AuthContext.js
    │   ├── pages/
    │   │   ├── Login.js
    │   │   ├── Register.js
    │   │   ├── AlumniWall.js
    │   │   ├── AlumniDetail.js
    │   │   ├── MyProfile.js
    │   │   └── AdminDashboard.js
    │   ├── services/
    │   │   └── api.js
    │   ├── App.js
    │   ├── index.js
    │   └── index.css
    └── package.json
```

## 启动步骤

### 1. 后端启动

```bash
# 进入后端目录
cd backend

# 安装依赖
npm install

# 初始化数据库（首次运行必须执行）
npm run init-db

# 启动后端服务（开发模式）
npm run dev

# 或生产模式
npm start
```

后端服务运行在: http://localhost:3001

### 2. 前端启动

打开新的终端窗口：

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动前端开发服务器
npm start
```

前端服务运行在: http://localhost:3000

## 默认账号

- 管理员账号: admin@alumni.com / admin123

## 主要功能

### 校友用户
- 注册和登录
- 填写和更新个人信息
- 记录工作变动
- 查看校友名片墙
- 按届别、城市、行业搜索校友
- 查看校友详情
- 接收并确认管理员的更新提醒

### 管理员
- 查看校友统计概览
- 按届别、城市、行业查看分布
- 向校友发送更新提醒
- 批量发送更新提醒
- 查看提醒记录
- 确认校友信息更新

## API 接口

### 认证接口
- POST /api/auth/register - 注册
- POST /api/auth/login - 登录
- POST /api/auth/logout - 登出

### 校友接口
- GET /api/alumni/me - 获取个人信息
- PUT /api/alumni/me - 更新个人信息
- GET /api/alumni/list - 获取校友列表（支持搜索和筛选）
- GET /api/alumni/:id - 获取校友详情
- POST /api/alumni/job-changes - 添加工作变动
- GET /api/alumni/stats/filters - 获取筛选选项

### 管理员接口
- GET /api/admin/dashboard - 获取仪表盘数据
- POST /api/admin/send-reminder - 发送单个提醒
- POST /api/admin/send-batch-reminders - 批量发送提醒
- GET /api/admin/reminders - 获取提醒列表
- GET /api/admin/alumni-without-recent-updates - 获取长期未更新校友
- POST /api/admin/confirm-update/:userId - 确认更新
